let a #= 0;
let b #= 0;
let c #= 0;

let sum $= #a + #b + #c;
let product $= #a * #b * #c;
let isTriplet $= #a * #a + #b * #b == #c * #c;

for(a #= 1; #a < 1000; #a++){
    for(b #= #a; #a + #b < 1000; #b++){
        c #= 1000 - #a - #b;
        if(#c > #b && $isTriplet){
            print($product)
        }
    }
}
