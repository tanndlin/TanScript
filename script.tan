def isPalindrome(n){
    let copy = n;
    let reversed = 0;

    while(n > 0){
        let digit = n % 10;
        n //= 10;
        reversed = reversed * 10 + digit;
    }

    reversed == copy;
}


let max = 0;

let a #= 100;
let nextA $= #a + 1;

let b #= 100;
let nextB $= #b + 1;

let product $= #a * #b;

for(a #= 100; #a < 1000; #a++){
    for(b #= 100; #b < 1000; #b++){
        if($product > max && isPalindrome($product)){
            max = $product;
        }
    }
}

max;